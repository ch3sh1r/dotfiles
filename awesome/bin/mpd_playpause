#!/bin/bash

STATUS=`mpc status | grep '\[' | cut -d']' -f 1`
if [[ ${STATUS:1} == "playing" ]]; then
    mpc pause
    notify-send "MPD playback paused."
else
    mpc play
    notify-send "$(mpc current)"
fi
