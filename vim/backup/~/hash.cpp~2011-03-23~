int f(int x)
{
    x = x + x;
}
typedef pair <int, int> Pair;
stack<Pair> s;
int n = 0;
int x = 0;
s.push(Pair(x, n));
for(;;)
{
	x = f(x); n++;
	while(!s.empty() && s.top().first < x)
		s.pop();
	if(s.top().first == x)
		return n - s.top().second;
	s.push(Pair(x, n));
}
